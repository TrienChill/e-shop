// Web Desktop: profile page rendered inside the account sidebar layout.
// Mobile: redirects to the tabs profile via the tabs layout href override.
export { default } from "../../(shop)/(tabs)/profile";
